<div class="card">
    <h2><a href="/post/{{id}}">{{title}}</a></h2>
    <h3>{{user.username}}<span>{{format_date date_created}}</span></h3>
    <p>{{post_text}}</p>

    <div>
        <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
    </div>
</div>
