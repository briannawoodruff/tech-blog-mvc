<div class="comment-card">
    {{#each this}}
    <section class="comment">
        <h5>
            {{user.username}}<span>posted on {{format_date date_created}}</span>
        </h5>
        <div>
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</div>